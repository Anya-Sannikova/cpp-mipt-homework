1) не добавлена библиотека stdio.h
2) не написан & перед а, при считывании надо указывать адрес переменной, а не ее название
3) написано = вместо ==. Выполняется присваивание, а не проверка
4) убрать \n, потому что \n считывает все до первого не пробельного символа
5) забыты {}, два условия надо уже объединять в блок
6) убрать ; в строке с if
7) в цикле не обновляется счетчик
8) убрать ; в строке с while (i <= 10)
9) у переменной sum изначально не было значения никакого
10) выход за границы массива
